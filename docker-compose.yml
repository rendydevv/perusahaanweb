version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: sixcompany-web:latest
    container_name: sixcompany-web
    ports:
      - "80:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - "com.sixcompany.description=SIX COMPANY Landing Page"
      - "com.sixcompany.version=1.0.0"

  # Development service (optional — for hot reload dev environment)
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: sixcompany-web-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    profiles:
      - dev
